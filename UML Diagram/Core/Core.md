```mermaid
    classDiagram
    class Position{
        +File Files
        +Rank Ranks
        +SameFile(Position) bool
        +SameRank(Position) bool
        +SamePosition(Position) bool
    }
    class Enum
```